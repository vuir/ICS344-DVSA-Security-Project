# Lesson 7 - Temporary Lambda instrumentation for controlled lab evidence only
# Add inside lambda_handler only long enough to capture redacted proof.
# Remove immediately after the evidence screenshot is captured.

import os

print(dict(os.environ))
